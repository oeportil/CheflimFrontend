export type Paso = {
    paso: string,
    orden: number
}

export type Receta = {

}

export interface IUser {
    id_usuario: number
    correo: string
    nombre: string
    token: string
    usuario: string
}